apiVersion: v1
kind: Service
metadata:
  name: s3proxy
  namespace: ${S3PROXY_NAMESPACE}
spec:
  type: ClusterIP
  selector:
    app: s3proxy
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    name: http
